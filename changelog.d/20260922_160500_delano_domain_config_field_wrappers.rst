.. A new scriv changelog fragment.

Fixed
-----

- In the colonel's domain configuration editor each field and its error
  message share one wrapper, so an error no longer overrides the form's
  spacing with a forced utility class.
