.. A new scriv changelog fragment.

Changed
-------

- The custom email sender form now pre-fills ``no-reply@<domain>`` (and the
  organization name as the sender display name) for domains that have no saved
  email configuration yet. Operators can enable the feature by flipping the
  toggle and saving, without first having to choose a sender address; both
  fields remain editable.
