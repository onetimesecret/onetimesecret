# Debug Scripts

Tools for analyzing and debugging locale file structural issues.

## Scripts
- `debug-locale-structure.sh` - Analyzes structural conflicts in a single locale
- `debug-all-locales` - Batch analysis of all locale files for conflicts

## Usage
Use these tools to identify type mismatches and missing keys before running harmonization.
Reports conflicts like: base=string vs target=object mismatches.
