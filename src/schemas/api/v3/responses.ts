// src/schemas/api/v3/responses.ts
//
// V3 inherits V2's response contracts. Re-exported here so existing
// V3 imports continue to resolve without changing call sites.

export * from '@/schemas/api/v2/responses';
