# Validate Scripts

Tools for checking locale file integrity and structure consistency.

## Scripts
- `check-locale-file` - Validates a single locale file structure
- `check-locale-files` - Batch validation of all locale files

## Usage
Run validation before committing changes to ensure all locale files maintain proper structure and have all required keys.

Exit codes: 0 = valid, 1 = issues found
