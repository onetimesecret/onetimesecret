# Plan Catalog Reference

**Auto-generated from:** `etc/billing.yaml`
**Schema Version:** 1.0
**Last Updated:** 2026-05-30 08:23 UTC

⚠️ **Do not edit this file directly.** This file is regenerated automatically:
- Every `pnpm dev` (via the `predev` hook)
- On demand: `pnpm run docs:billing:generate` or `bin/ots billing catalog generate-docs`

Source of truth is `etc/billing.yaml`. Edit that file, then any of the
above will refresh this doc. Stale committed output is treated as a
drift bug — keep it synchronized.

## Overview

This document describes the billing plan structure and entitlements for Onetime Secret. Plan definitions are stored in Stripe product metadata and cached in Redis via `Billing::Plan`.


## Entitlement Definitions

Entitlements define features/permissions available in the billing system.
Loaded from `etc/billing.yaml`.

### Advanced

- **`ip_access_rules`**: IP-based access restrictions
- **`audit_logs`**: Access to audit log features
- **`manage_sso`**: Single sign-on configuration and management
- **`custom_signup_validation`**: Per-domain signup validation strategy (passthrough, allowlist, MX, SMTP)

### Branding

- **`custom_branding`**: Custom branding and styling
- **`workspace_branding`**: Full workspace branding (login pages, email templates)
- **`custom_mail_sender`**: Custom email sender address for domain-specific notifications
- **`flexible_from_domain`**: Allows setting any domain in email sender From Address (not restricted to custom domain)

### Collaboration

- **`manage_orgs`**: Can create and manage organizations
- **`manage_teams`**: Can create and manage teams
- **`manage_members`**: Can create and manage organization members

### Core

- **`create_secrets`**: Can create basic secrets
- **`view_receipt`**: Can view secret metadata
- **`homepage_secrets`**: Receive secrets via branded homepage (public/private/disabled)
- **`incoming_secrets`**: Receive secrets via direct requests (public/private/disabled)
- **`notifications`**: Email notifications for secret activity
- **`custom_privacy_defaults`**: Custom privacy defaults for secrets
- **`extended_default_expiration`**: Extended secret expiration options beyond standard limits

### Infrastructure

- **`api_access`**: Can use REST API endpoints
- **`custom_domains`**: Can configure custom domains


## Plans Overview

| Plan ID | Name | Tier | Tenancy | Region | Display Order | On Plans Page | Legacy |
|---------|------|------|---------|--------|---------------|---------------|--------|
| free_v1 | Free (CA) | free | multi | CA | 0 | ✓ |  |
| identity | Legacy Plan (Legacy) | single_account | multi | CA |  | ✗ | ⚠️ |
| identity_plus_v1 | Identity Plus (CA) | single_account | multi | CA | 10 | ✓ |  |
| team_plus_v1 | Team Plus (CA) | single_team | multi | CA | 30 | ✓ |  |

## Plan Details

### Free (CA) (`free_v1`)

Default tier for all users without subscription

**Tier:** free
**Tenancy:** multi
**Region:** CA

**Entitlements:**
- `api_access`
- `create_secrets`
- `custom_domains`
- `homepage_secrets`
- `incoming_secrets`
- `view_receipt`

**Limits:**

| Resource | Limit | Notes |
|----------|-------|-------|
| organizations | ∞ (unlimited) |  |
| members_per_org_max | 1 |  |
| members_per_org | 1 |  |
| role_members_per_org | 1 |  |
| custom_domains | 1 |  |
| secret_lifetime | 1209600 | 14 days |
| secrets_per_day | TBD |  |

**Pricing:** Free

---

### Legacy Plan (Legacy) (`identity`) ⚠️ **(Legacy)**

Original plan, grandfathered for existing customers

**Grandfathered Until:** 2028-01-31

**Tier:** single_account
**Tenancy:** multi
**Region:** CA

**Entitlements:**
- `api_access`
- `create_secrets`
- `custom_branding`
- `custom_domains`
- `custom_mail_sender`
- `custom_privacy_defaults`
- `homepage_secrets`
- `incoming_secrets`
- `manage_members`
- `manage_orgs`
- `view_receipt`

**Limits:**

| Resource | Limit | Notes |
|----------|-------|-------|
| custom_domains | ∞ (unlimited) |  |
| organizations | 10 |  |
| role_admins_per_org | 0 |  |
| role_members_per_org | ∞ (unlimited) |  |
| role_owners_per_org | 1 |  |
| secret_lifetime | 2592000 | 30 days |
| secrets_per_day | ∞ (unlimited) |  |
| total_members_per_org | 50 |  |


---

### Identity Plus (CA) (`identity_plus_v1`)

For individuals needing custom domains and extended retention

**Tier:** single_account
**Tenancy:** multi
**Region:** CA

**Entitlements:**
- `api_access`
- `create_secrets`
- `custom_branding`
- `custom_domains`
- `custom_mail_sender`
- `custom_privacy_defaults`
- `extended_default_expiration`
- `homepage_secrets`
- `incoming_secrets`
- `manage_members`
- `manage_orgs`
- `view_receipt`

**Limits:**

| Resource | Limit | Notes |
|----------|-------|-------|
| custom_domains | ∞ (unlimited) |  |
| organizations | 10 |  |
| role_admins_per_org | 0 |  |
| role_members_per_org | ∞ (unlimited) |  |
| role_owners_per_org | 1 |  |
| secret_lifetime | 2592000 | 30 days |
| secrets_per_day | ∞ (unlimited) |  |
| total_members_per_org | 50 |  |

**Pricing:**
- Monthly: $9.0 CAD
- Annual: $90.0 CAD

---

### Team Plus (CA) (`team_plus_v1`)

For teams needing collaboration features

**Tier:** single_team
**Tenancy:** multi
**Region:** CA

**Entitlements:**
- `api_access`
- `create_secrets`
- `custom_branding`
- `custom_domains`
- `custom_mail_sender`
- `custom_privacy_defaults`
- `custom_signup_validation`
- `extended_default_expiration`
- `flexible_from_domain`
- `homepage_secrets`
- `incoming_secrets`
- `ip_access_rules`
- `manage_members`
- `manage_orgs`
- `manage_sso`
- `manage_teams`
- `view_receipt`
- `workspace_branding`

**Limits:**

| Resource | Limit | Notes |
|----------|-------|-------|
| custom_domains | ∞ (unlimited) |  |
| organizations | 10 |  |
| role_admins_per_org | ∞ (unlimited) |  |
| role_members_per_org | ∞ (unlimited) |  |
| role_owners_per_org | 1 |  |
| secret_lifetime | 2592000 | 30 days |
| secrets_per_day | ∞ (unlimited) |  |
| total_members_per_org | 1 | Individual only |

**Pricing:**
- Monthly: $85.0 CAD
- Annual: $850.0 CAD

---


## Stripe Product Configuration

Each Stripe product must include specific metadata fields to be recognized by the billing system.

### Required Metadata Fields

```json
{
  "app": "onetimesecret",
  "tier": "Tier identifier (single_account, single_team, multi_team, etc.)",
  "tenancy": "Tenancy type (multi, dedicated)"
}
```

### Optional Metadata Fields

```json
{
  "plan_id": "Unique plan identifier (auto-generated if not provided)",
  "region": "Regional scope (e.g., EU, US, CA); omit for global plans",
  "display_order": "Sort order on plans page (lower = earlier, default: 0)",
  "show_on_plans_page": "Visibility on plans page (true/false, default: true)",
  "entitlements": "Comma-separated entitlement list",
  "created": "ISO 8601 creation timestamp",
  "limit_members_per_org_max": "Max members per org (-1 for unlimited)",
  "limit_custom_domains": "Max custom domains (-1 for unlimited)",
  "limit_secret_lifetime": "Max secret lifetime in seconds (-1 for unlimited)",
  "limit_secrets_per_day": "Daily secret creation limit (-1 for unlimited)"
}
```

## Validation and Sync

### Validate Catalog Structure

Validate YAML structure and compare with Stripe:

```bash
bin/ots billing catalog validate
bin/ots billing catalog validate --catalog-only  # Skip Stripe comparison
bin/ots billing catalog validate --strict        # Fail on warnings
```

### Sync Plans from Stripe

After configuring products in Stripe, sync to local Redis cache:

```bash
bin/ots billing sync
bin/ots billing sync --clear  # Clear cache first
```

Only products with all required metadata fields, at least one recurring price, and `app: "onetimesecret"` will be synced.

### View Cached Plans

```bash
bin/ots billing plans
```

## Setup Workflow

1. **Edit catalog:** Modify `etc/billing.yaml`
2. **Validate:** `bin/ots billing catalog validate --catalog-only`
3. **Create/update in Stripe:** `./scripts/setup_stripe_plans.sh --update`
4. **Verify in Stripe:** Check Stripe Dashboard
5. **Sync to cache:** `bin/ots billing sync`
6. **Update docs:** `bin/ots billing catalog generate-docs`
7. **Commit:** Commit both YAML and generated docs
