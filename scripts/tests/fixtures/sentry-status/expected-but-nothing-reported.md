
## Sentry delivery

| Component | Result | Detail |
| --- | --- | --- |
| frontend-assets | **BLOCKED** | `the step never reported a result, so delivery state is unknown and must be treated as not shipped` |
| release | **BLOCKED** | `the step never reported a result, so delivery state is unknown and must be treated as not shipped` |

> Sourcemap/release delivery is degraded for: frontend-assets,release.
> The image build is unaffected (Sentry steps are non-blocking), but
> stack traces for this release will be unsymbolicated or misfiled.
