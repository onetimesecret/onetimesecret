#!/usr/bin/env bash
# bin/console
#
# Env comes from the shell (direnv via .envrc handles .env/.env.local/.env.test
# ordering). Sourcing .env here would clobber .env.local overrides.

bundle exec bin/ots console
