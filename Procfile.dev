# Procfile.dev

# The "sh -c" Explicitly invokes a shell to interpret the command
backend: sh -c 'bundle exec puma -p $${PORT:-3000} -t 2:4 -w 0 -e development'

frontend: pnpm run dev

worker: bundle exec bin/ots jobs worker
