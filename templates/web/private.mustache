{{>header}}

  <div class="private">
    {{#show_secret}}
    <div class="uri">
      <span class="pretext">Share this link:</span><br/>
      <input id="secreturi" class="selectable" value="{{share_uri}}"/>
      <p>This link can only be accessed one time. Expires in {{expiration_stamp}}.</p>
    </div>
    {{#show_passphrase}}
    <div class="passphrase">
      <span class="pretext">Passphrase:</span><br/>
      <input class="selectable" value="{{passphrase_temp}}" />
      <p>(the recipient will need this to view the secret.)</p>
    </div>
    {{/show_passphrase}}
    <div class="secret">
      <span class="pretext">Secret:</span><br/>
      {{^can_decrypt}}
      <em class="lighter">This secret is encrypted with your passphrase.</em>
      {{/can_decrypt}}
      {{#can_decrypt}}
      {{#one_liner}}
        <input class="selectable" id="displayedsecret" value="{{secret_value}}" disabled="disabled" />
        <p>(careful: it will disappear when you refresh this page.)</p>
      {{/one_liner}}
      {{^one_liner}}
        <p>(careful: it will disappear when you refresh this page.)</p>
        <textarea class="selectable" rows="{{display_lines}}" cols="80">{{secret_value}}</textarea>
        {{#truncated}}
        <p>Note: this secret was truncated b/c it was over 5KB</p>
        {{/truncated}}
      {{/one_liner}}
      {{/can_decrypt}}
    </div>
    {{/show_secret}}
    
    {{^show_secret}}
    <div class="secret">
      <span class="pretext">Secret:</span><br/>
      <input value="*******************" disabled="disabled" />
      {{#been_shared}}
      <p><em>This secret was received {{shared_date}}.</em></p>
      {{/been_shared}}
      {{^been_shared}}
      <p><em>This secret has not been received yet. Expires in {{expiration_stamp}}.</em></p>
      {{/been_shared}}
    </div>
    <div id="faq" class="smaller">
      <h3>F.A.Q.</h3>
      <h4>What if I forgot to copy the shared link?</h4>
      <p>You need to create a new secret. We can't retrieve it for you.</p>
      
      <h4>How long with the secret be available?</h4>
      <p>The secret link will be available for 7 days or until it's viewed.</p>
    </div>
    {{/show_secret}}
  </div>

{{>footer}}
