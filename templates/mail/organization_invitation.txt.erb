You've been invited to join <%= organization_name %>!

<%= inviter_email %> has invited you to join "<%= organization_name %>" as <%= role_description %>.

To accept this invitation, click the link below:

<%= baseuri %><%= invite_uri %>

This invitation will expire in <%= expires_in_days %> days.

If you don't want to join, you can simply ignore this email or decline the invitation.

Delano
<%= baseuri %>

P.S. This email was sent to <%= invited_email %>. If you weren't expecting this invitation, you can safely ignore this message.
