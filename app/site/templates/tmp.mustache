{{>header}}

  <div class="shared">
    {{#show_secret}}
    <div class="secret">
      <span class="context">This secret is for you:</span><br/>
      <input value="{{ssecret.value}}" />
      <p>(careful: it will disappear when you refresh.)</p>
    </div>
    {{/show_secret}}
  </div>

{{>footer}}