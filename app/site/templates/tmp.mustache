{{>header}}

  <div class="private">
    <div class="secret">
      <span class="context">SECRET:</span><br/>
      {{#show_secret}}
      <input value="{{ssecret.value}}" /> (<em>careful: we'll only show it this once</em>)
      {{/show_secret}}
      {{^show_secret}}
      <em>Already shown once before.</em>
      {{/show_secret}}
    </div>
    <div class="uri">
      <span class="context">share this uri:</span><br/>
      <input value="{{share_uri}}"/>
    </div>
    <div class="uri">
      <span class="context">your private uri:</span><br/>
      <input value="{{admin_uri}}" />
    </div>
  </div>



{{>footer}}